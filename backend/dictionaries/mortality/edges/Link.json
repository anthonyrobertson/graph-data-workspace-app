{
    "$id": "Link",
    "$schema": "http://json-schema.org/draft-07/schema#",
    "type": "object",
    "title": "Link",
    "properties": {
      "type": { "const": "Link" },
      "description": { "type": "string"},
      "source": { "type": "string", "description": "arbitrary link between nodes" },
      "target": { "type": "string", "description": "arbitrary link between nodes" },
      "metadata": { "$ref": "metadataSchema" }
    },
    "required": ["type", "source", "target", "metadata"]
  }
  